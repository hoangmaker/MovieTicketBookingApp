/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import MainScreens from './src/screens/main';

AppRegistry.registerComponent(appName, () => MainScreens);
